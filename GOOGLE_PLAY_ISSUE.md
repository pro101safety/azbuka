# Решение проблемы с нативными библиотеками в Google Play Console

## ⚠️ Ситуация: Предупреждения в Google Play Console

Google Play Console показывает **предупреждения** о нативных библиотеках:
- `base/lib/arm64-v8a/libc++_shared.so`
- `base/lib/arm64-v8a/libjniPdfium.so`
- `base/lib/arm64-v8a/libmodft2.so`
- `base/lib/arm64-v8a/libmodpdfium.so`
- `base/lib/arm64-v8a/libmodpng.so`

## 🔍 Причина предупреждений

**Проблема:** Библиотека `android-pdf-viewer:3.2.0-beta.3` содержит нативные библиотеки, которые могут быть не выровнены на 16 КБ. Google Play Console предупреждает, что эти библиотеки могут не работать корректно на устройствах с 16 КБ страницами.

## ✅ Решение

### Шаг 1: Обновлены настройки сборки

В `app/build.gradle` уже добавлены правильные настройки:
- ✅ `extractNativeLibs="false"` в манифесте
- ✅ `useLegacyPackaging = false` в build.gradle
- ✅ Android Gradle Plugin 8.13.0 автоматически выравнивает библиотеки

### Шаг 2: Пересоберите проект

```bash
# Очистите проект
gradlew clean

# Пересоберите release AAB
gradlew bundleRelease
```

### Шаг 3: Проверьте результат через APK Analyzer

**Согласно [статье на Habr](https://habr.com/ru/companies/ncloudtech/articles/960520/), это самый надежный способ проверки:**

1. **Соберите release APK:**
   ```bash
   gradlew assembleRelease
   ```

2. **Откройте APK Analyzer:**
   - Android Studio → Build → Analyze APK
   - Выберите: `app/build/outputs/apk/release/app-release.apk`

3. **Проверьте выравнивание:**
   - Откройте раздел **"lib"** или **"Native libs"**
   - В столбце **"Alignment"** должно быть **16384** (16 КБ) для всех `.so` файлов
   - Если видите **4096** (4 КБ) — библиотека не выровнена

4. **Если выравнивание 16384** — все правильно, AGP 8.13.0 обработал библиотеки

5. **Загрузите AAB в Google Play Console** — предупреждения должны исчезнуть или стать информационными

**Важно:** Используйте Android Studio 2025.1.3 или новее для корректной проверки выравнивания.

## Решения

### Решение 1: Проверка контекста сообщения

**Важно:** Уточните, что именно показывает Google Play Console:
- ❌ **Ошибка** (красный цвет) — приложение отклонено
- ⚠️ **Предупреждение** (желтый цвет) — можно игнорировать или исправить
- ℹ️ **Информация** (синий/серый цвет) — просто показывает содержимое AAB

### Решение 2: Проверка библиотеки PDF Viewer

Библиотека `com.github.mhiew:android-pdf-viewer:3.2.0-beta.3` может содержать нативные библиотеки, которые не выровнены на 16 КБ.

**Проверьте:**
1. Есть ли обновленная версия библиотеки
2. Поддерживает ли библиотека 16 КБ страницы

### Решение 3: Принудительное выравнивание (если требуется)

Если Google Play требует выравнивание, можно добавить настройку в `build.gradle`:

```gradle
android {
    packaging {
        jniLibs {
            useLegacyPackaging = false
            // Дополнительные настройки для выравнивания
        }
    }
}
```

### Решение 4: Обновление зависимостей

Проверьте, есть ли обновления для библиотеки PDF viewer, которые поддерживают 16 КБ страницы.

## Что делать сейчас

1. **Проверьте тип сообщения в Google Play Console:**
   - Откройте раздел, где показаны библиотеки
   - Посмотрите, есть ли рядом иконка предупреждения или ошибки
   - Прочитайте полный текст сообщения

2. **Проверьте статус загрузки:**
   - Если AAB загружен успешно — это информационное сообщение
   - Если есть ошибки — нужно исправить

3. **Проверьте раздел "App bundle explorer":**
   - Google Play Console → Release → Production → App bundle explorer
   - Там будет показана детальная информация о библиотеках

## Дополнительная информация

Если это **предупреждение** о выравнивании, Android Gradle Plugin 8.13.0 должен автоматически выравнивать библиотеки при правильных настройках:
- ✅ `extractNativeLibs="false"` — установлено
- ✅ `useLegacyPackaging = false` — установлено
- ✅ AGP 8.13.0 — автоматически выравнивает библиотеки

## Следующие шаги

1. Уточните тип сообщения в Google Play Console
2. Если это ошибка — пришлите полный текст ошибки
3. Если это предупреждение — можно попробовать обновить библиотеку PDF viewer
4. Если это просто информация — все в порядке, можно продолжать публикацию

