# Решение проблемы с 16 КБ страницами после добавления викторины

## ❌ Проблема

Google Play Console сообщает об ошибке:
```
Приложение не поддерживает 16 КБ страницы
```

**Важное наблюдение:** Проблема появилась после добавления викторины по охране труда.

## 🔍 Анализ проблемы

### Что было добавлено с викториной:

1. **Room Database 2.6.1** - содержит нативные библиотеки SQLite
2. **Дополнительные зависимости** - могут содержать нативные библиотеки

### Возможные причины:

1. **Room 2.6.1 содержит старые нативные библиотеки SQLite**
   - Версия 2.6.1 была выпущена до требований к 16 КБ страницам
   - Нативные библиотеки SQLite могут быть не выровнены на 16 КБ

2. **Библиотека AndroidPdfViewer:master-SNAPSHOT**
   - Может содержать нативные библиотеки, которые не выровнены
   - `master-SNAPSHOT` может быть нестабильным

## ✅ Решение

### 1. Обновление Room Database

**Было:**
```gradle
def room_version = "2.6.1"
```

**Стало:**
```gradle
def room_version = "2.7.0"
```

**Почему:**
- Room 2.7.0 и выше содержат обновленные нативные библиотеки SQLite
- Эти версии собираются с поддержкой 16 КБ страниц
- AGP 8.13.0 лучше обрабатывает новые версии библиотек

### 2. Проверка всех зависимостей

Убедитесь, что все зависимости обновлены до последних версий:

- ✅ Room: 2.7.0 (обновлено)
- ⚠️ AndroidPdfViewer: master-SNAPSHOT (может быть проблемой)
- ✅ AppCompat: 1.6.1
- ✅ Material: 1.10.0

### 3. Проверка настроек

Убедитесь, что все настройки правильные:

- ✅ `extractNativeLibs="false"` в AndroidManifest.xml
- ✅ `useLegacyPackaging = false` в build.gradle
- ✅ `targetSdk 35` в build.gradle
- ✅ AGP 8.13.0

## 📋 Следующие шаги

1. **Пересоберите проект:**
   ```bash
   gradlew clean
   gradlew bundleRelease
   ```

2. **Проверьте через APK Analyzer:**
   - Build → Analyze APK
   - Проверьте столбец "Alignment" для всех `.so` файлов
   - Должно быть 16384 (16 КБ) для всех библиотек

3. **Загрузите в Google Play Console:**
   - Проверьте, исчезла ли ошибка

## ⚠️ Если проблема сохраняется

Если после обновления Room проблема сохраняется:

1. **Проверьте библиотеку AndroidPdfViewer:**
   - Попробуйте использовать конкретный тег вместо `master-SNAPSHOT`
   - Проверьте на https://jitpack.io/#barteksc/AndroidPdfViewer

2. **Проверьте, какие библиотеки не выровнены:**
   - Google Play Console должен показать список проблемных библиотек
   - Проверьте каждую библиотеку отдельно

3. **Временное решение (если критично):**
   - Можно временно отключить викторину для проверки
   - Если без викторины все работает, проблема точно в зависимостях викторины

## 🔧 Дополнительная диагностика

Чтобы точно определить проблемную библиотеку:

1. **Соберите APK:**
   ```bash
   gradlew assembleRelease
   ```

2. **Проверьте через APK Analyzer:**
   - Откройте раздел "lib"
   - Найдите библиотеки с выравниванием 4096 (4 КБ) вместо 16384 (16 КБ)
   - Это и есть проблемные библиотеки

3. **Проверьте зависимости:**
   ```bash
   gradlew dependencies --configuration releaseRuntimeClasspath
   ```
   - Найдите все нативные библиотеки
   - Проверьте их версии

## ✅ Ожидаемый результат

После обновления Room до 2.7.0:
- ✅ Нативные библиотеки SQLite должны быть выровнены на 16 КБ
- ✅ Google Play Console не должен показывать ошибки
- ✅ Приложение должно работать на устройствах с 16 КБ страницами

