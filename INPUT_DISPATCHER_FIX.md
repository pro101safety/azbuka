# Решение проблемы InputDispatcher: Channel is unrecoverably broken

## ❌ Проблема

Ошибки в логах:
```
InputDispatcher: Channel is unrecoverably broken and will be disposed!
```

Это происходит в:
- `MainActivity`
- `QuizMainActivity`

## 🔍 Причина

Ошибка `InputDispatcher: Channel is unrecoverably broken` обычно означает, что приложение крашится из-за:

1. **Необработанных исключений** (NullPointerException, IllegalStateException и т.д.)
2. **Проблем с обработкой событий ввода** (кнопка "Назад", клики)
3. **Устаревших методов** (`onBackPressed()` устарел в Android 13+)
4. **Проблем с findViewById** (возвращает null)

## ✅ Исправления

### 1. Добавлены проверки на null для findViewById

**MainActivity.java:**
- Добавлена проверка для `expandableListView`
- Добавлена проверка для `rootView`

**QuizMainActivity.java:**
- Добавлена проверка для `categoryGridView`

### 2. Исправлен метод onBackPressed()

**Проблема:** `onBackPressed()` устарел в Android 13+ (API 33+)

**Решение:**
- Добавлен `OnBackPressedCallback` для Android 13+
- Сохранена обратная совместимость для старых версий

### 3. Добавлена защита от крашей

Если критический элемент не найден, Activity корректно завершается вместо краша.

## 📋 Что было исправлено

1. ✅ Проверки на null для всех `findViewById`
2. ✅ Правильная обработка кнопки "Назад" для Android 13+
3. ✅ Защита от крашей при отсутствии элементов UI

## 🔧 Дополнительные рекомендации

Если проблема сохраняется:

1. **Проверьте логи через Logcat:**
   ```bash
   adb logcat | grep -i "FATAL\|Exception\|Error"
   ```

2. **Проверьте, нет ли других необработанных исключений:**
   - Проверьте все `findViewById` на наличие проверок
   - Проверьте обработчики событий на null

3. **Проверьте, связана ли проблема с заменой библиотеки PDF viewer:**
   - Если ошибки начались после замены библиотеки, возможно, проблема в ней
   - Проверьте, что библиотека правильно инициализируется

## ⚠️ Важно

После исправлений:
1. Пересоберите проект: `gradlew clean build`
2. Протестируйте на реальном устройстве
3. Проверьте логи через Logcat для выявления других проблем

