# Использование master-SNAPSHOT для библиотеки AndroidPdfViewer

## ❌ Проблема

Версия `2.8.2` не найдена в JitPack:
```
Could not find com.github.barteksc:AndroidPdfViewer:2.8.2
```

## ✅ Решение

Использование `master-SNAPSHOT` для получения последнего коммита из master ветки.

### Формат для JitPack

```gradle
implementation 'com.github.barteksc:AndroidPdfViewer:master-SNAPSHOT'
```

**Как это работает:**
- JitPack автоматически соберет библиотеку из последнего коммита в master ветке
- Это гарантирует, что вы получите самую свежую версию
- JitPack кэширует сборку, поэтому последующие сборки будут быстрее

## ⚠️ Важно

### Преимущества:
- ✅ Всегда последняя версия
- ✅ Работает, даже если конкретные теги недоступны
- ✅ JitPack автоматически собирает библиотеку

### Недостатки:
- ⚠️ Версия может измениться при следующей синхронизации
- ⚠️ Может быть нестабильной (если в master есть нестабильный код)

## 🔧 Альтернативные варианты

Если `master-SNAPSHOT` не работает, попробуйте:

### Вариант 1: Конкретный коммит (SHA)
```gradle
// Найдите SHA коммита на GitHub и используйте его
implementation 'com.github.barteksc:AndroidPdfViewer:COMMIT_SHA'
```

### Вариант 2: Конкретный тег
Проверьте доступные теги на:
- https://github.com/barteksc/AndroidPdfViewer/releases
- https://jitpack.io/#barteksc/AndroidPdfViewer

Используйте один из доступных тегов:
```gradle
implementation 'com.github.barteksc:AndroidPdfViewer:3.0.0-beta.5'
```

### Вариант 3: Вернуться к оригинальной библиотеке
Если проблемы продолжаются, можно вернуться к `mhiew:android-pdf-viewer`, но это не решит проблему с 16 КБ:
```gradle
implementation 'com.github.mhiew:android-pdf-viewer:3.2.0-beta.3'
```

## 📋 Следующие шаги

1. **Синхронизируйте проект:**
   - Android Studio → File → Sync Project with Gradle Files

2. **Если master-SNAPSHOT не работает:**
   - Проверьте доступные версии на https://jitpack.io/#barteksc/AndroidPdfViewer
   - Используйте конкретный тег или SHA коммита

3. **После успешной сборки:**
   - Проверьте, что библиотека работает корректно
   - Протестируйте открытие PDF файлов

## 🔍 Проверка доступных версий

Чтобы проверить доступные версии:

1. Откройте: https://jitpack.io/#barteksc/AndroidPdfViewer
2. Посмотрите список доступных тегов
3. Используйте один из доступных тегов вместо `master-SNAPSHOT`

