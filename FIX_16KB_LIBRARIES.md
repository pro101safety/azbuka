# Решение проблемы с библиотеками без поддержки 16 КБ

## ❌ Проблема

Google Play Console сообщает об ошибке:
```
Не поддерживает 16 КБ

Библиотеки без поддержки 16 КБ:
- base/lib/arm64-v8a/libc++_shared.so
- base/lib/arm64-v8a/libjniPdfium.so
- base/lib/arm64-v8a/libmodft2.so
- base/lib/arm64-v8a/libmodpdfium.so
- base/lib/arm64-v8a/libmodpng.so
```

Эти библиотеки приходят из зависимости `android-pdf-viewer:3.2.0-beta.3`.

## 🔍 Причина

Библиотека `android-pdf-viewer` была скомпилирована до появления требований к 16 КБ страницам. AGP 8.13.0 автоматически выравнивает библиотеки, которые собираются в вашем проекте, но **не всегда может перевыровнять уже скомпилированные библиотеки из зависимостей**.

## ✅ Решения

### Решение 1: Проверьте обновление библиотеки (первый шаг)

1. **Откройте GitHub репозиторий:**
   - https://github.com/mhiew/android-pdf-viewer
   - Проверьте последние релизы и issues

2. **Проверьте issues на тему 16 КБ:**
   - Поищите issues с ключевыми словами: "16KB", "page size", "alignment"
   - Возможно, уже есть обновление или решение

3. **Попробуйте обновить библиотеку:**
   ```gradle
   // В app/build.gradle
   implementation 'com.github.mhiew:android-pdf-viewer:3.2.0-beta.3'
   // Попробуйте более новую версию, если доступна
   ```

### Решение 2: Используйте альтернативную библиотеку

Если библиотека не поддерживает 16 КБ, рассмотрите альтернативы:

#### Вариант A: PdfViewPager
```gradle
implementation 'es.voghdev.pdfviewpager:library:1.1.2'
```

#### Вариант B: AndroidPdfViewer
```gradle
implementation 'com.github.barteksc:android-pdf-viewer:3.2.0-beta.1'
```

#### Вариант C: WebView для простых PDF
Для простых PDF файлов можно использовать встроенный WebView:
```java
WebView webView = findViewById(R.id.webView);
webView.getSettings().setJavaScriptEnabled(true);
webView.loadUrl("file:///android_asset/your_file.pdf");
```

### Решение 3: Создайте форк библиотеки (если нужно срочно)

Если нужно срочно решить проблему:

1. **Создайте форк библиотеки на GitHub**
2. **Обновите настройки сборки** для поддержки 16 КБ:
   - В `CMakeLists.txt` добавьте: `set(CMAKE_SHARED_LINKER_FLAGS "${CMAKE_SHARED_LINKER_FLAGS} -Wl,-z,max-page-size=0x4000")`
   - Или в `build.gradle` библиотеки добавьте соответствующие настройки
3. **Пересоберите библиотеку**
4. **Используйте форк в вашем проекте**

### Решение 4: Обратитесь к разработчикам библиотеки

Создайте issue на GitHub с запросом поддержки 16 КБ страниц. Это поможет не только вам, но и другим разработчикам.

## 📋 Пошаговый план действий

1. **Проверьте обновление библиотеки:**
   - [ ] Откройте GitHub репозиторий android-pdf-viewer
   - [ ] Проверьте последние версии
   - [ ] Проверьте issues на тему 16 КБ

2. **Попробуйте обновить:**
   - [ ] Обновите библиотеку до последней версии
   - [ ] Пересоберите проект: `gradlew clean bundleRelease`
   - [ ] Проверьте через APK Analyzer выравнивание

3. **Если обновление не помогло:**
   - [ ] Рассмотрите альтернативные библиотеки
   - [ ] Или создайте issue на GitHub с запросом поддержки 16 КБ

4. **После изменений:**
   - [ ] Пересоберите проект
   - [ ] Проверьте через APK Analyzer (выравнивание должно быть 16384)
   - [ ] Загрузите новый AAB в Google Play Console

## ⚠️ Важно

**С 1 ноября 2025 года** Google Play будет **отклонять** приложения, которые не поддерживают 16 КБ страницы. Необходимо решить эту проблему до этого срока.

## 🔧 Техническая информация

### Почему AGP не выравнивает библиотеки из зависимостей?

AGP 8.5.1+ автоматически выравнивает нативные библиотеки, но это работает только для:
- ✅ Библиотек, которые собираются в вашем проекте
- ✅ Библиотек, которые уже выровнены на 16 КБ

Для уже скомпилированных библиотек из зависимостей AGP **не может перевыровнять** их, если они были скомпилированы с неправильным выравниванием (4 КБ вместо 16 КБ).

### Что можно сделать?

1. **Обновить библиотеку** - если разработчики добавили поддержку 16 КБ
2. **Использовать альтернативу** - если обновление недоступно
3. **Создать форк** - если нужно срочно решить проблему
4. **Обратиться к разработчикам** - чтобы они добавили поддержку

## 📞 Контакты для решения проблемы

- **GitHub репозиторий:** https://github.com/mhiew/android-pdf-viewer
- **Создайте issue:** с запросом поддержки 16 КБ страниц
- **Проверьте существующие issues:** возможно, уже есть решение

