# Решение ошибки "Не поддерживает 16 КБ" в Google Play Console

## ❌ Проблема

Google Play Console сообщает об ошибке:
```
Не поддерживает 16 КБ

Библиотеки без поддержки 16 КБ:
- base/lib/arm64-v8a/libc++_shared.so
- base/lib/arm64-v8a/libjniPdfium.so
- base/lib/arm64-v8a/libmodft2.so
- base/lib/arm64-v8a/libmodpdfium.so
- base/lib/arm64-v8a/libmodpng.so
```

**Причина:** Библиотека `android-pdf-viewer:3.2.0-beta.3` содержит нативные библиотеки, которые были скомпилированы без поддержки 16 КБ страниц. AGP 8.13.0 не всегда может автоматически перевыровнять уже скомпилированные библиотеки из зависимостей.

## ✅ Решения

### Решение 1: Обновление библиотеки (рекомендуется)

1. **Проверьте, есть ли обновление библиотеки:**
   - Откройте [GitHub репозиторий android-pdf-viewer](https://github.com/mhiew/android-pdf-viewer)
   - Проверьте последние версии и issues, связанные с 16 КБ поддержкой

2. **Попробуйте обновить до последней версии:**
   ```gradle
   implementation 'com.github.mhiew:android-pdf-viewer:3.2.0-beta.3'
   // Или попробуйте более новую версию, если доступна
   ```

### Решение 2: Использование альтернативной библиотеки

Если библиотека не поддерживает 16 КБ, рассмотрите альтернативы:

1. **PdfViewPager** - альтернативная библиотека для просмотра PDF
2. **AndroidPdfViewer** - другая библиотека для работы с PDF
3. **Встроенный WebView** - для простых PDF файлов можно использовать WebView

### Решение 3: Принудительное выравнивание через Gradle task

Добавьте Gradle task для выравнивания библиотек после сборки (см. `app/build.gradle`).

### Решение 4: Использование инструмента для выравнивания

Используйте инструмент `zipalign` или `readelf` для выравнивания библиотек после сборки.

## 🔧 Технические детали

### Почему AGP не выравнивает библиотеки из зависимостей?

AGP 8.5.1+ автоматически выравнивает нативные библиотеки, но это работает только для:
- Библиотек, которые собираются в вашем проекте
- Библиотек, которые уже выровнены на 16 КБ

Для уже скомпилированных библиотек из зависимостей AGP не всегда может перевыровнять их, если они были скомпилированы с неправильным выравниванием.

### Что делать?

1. **Обратитесь к разработчикам библиотеки** - попросите обновить библиотеку с поддержкой 16 КБ
2. **Используйте альтернативную библиотеку** - если обновление недоступно
3. **Создайте форк библиотеки** - если нужно срочно решить проблему

## 📋 Чек-лист действий

- [ ] Проверить последнюю версию `android-pdf-viewer` на GitHub
- [ ] Проверить issues библиотеки на тему 16 КБ поддержки
- [ ] Попробовать обновить библиотеку до последней версии
- [ ] Если обновление недоступно - рассмотреть альтернативные библиотеки
- [ ] Пересобрать проект после изменений
- [ ] Проверить через APK Analyzer, что выравнивание 16384
- [ ] Загрузить новый AAB в Google Play Console

## ⚠️ Важно

**С 1 ноября 2025 года** Google Play будет отклонять приложения, которые не поддерживают 16 КБ страницы. Необходимо решить эту проблему до этого срока.

