buildscript {
    repositories {
        google()
        mavenCentral()
    }
    dependencies {
        classpath 'com.android.tools.build:gradle:9.0.0' // Совместимо с Gradle 8.13
    }
}

task clean(type: Delete) {
    delete rootProject.buildDir
}