# Решение проблемы с версией библиотеки android-pdf-viewer

## ❌ Проблема

Ошибка при сборке:
```
Could not find com.github.barteksc:android-pdf-viewer:3.2.0-beta.1.
```

## 🔍 Причина

Версия `3.2.0-beta.1` может быть недоступна в JitPack или не существует. Для JitPack нужно использовать теги из GitHub репозитория.

## ✅ Решение

### Вариант 1: Использовать стабильную версию 2.8.2 (рекомендуется)

```gradle
implementation 'com.github.barteksc:android-pdf-viewer:2.8.2'
```

**Преимущества:**
- ✅ Стабильная версия
- ✅ Проверенная и рабочая
- ✅ Доступна в JitPack

**Проверка поддержки 16 КБ:**
- Версия 2.8.2 может не поддерживать 16 КБ страницы напрямую
- Но AGP 8.13.0 должен автоматически выровнять библиотеки

### Вариант 2: Использовать последний коммит (если нужна поддержка 16 КБ)

Если версия 2.8.2 не поддерживает 16 КБ, можно использовать последний коммит:

```gradle
// Используйте SHA коммита из GitHub
implementation 'com.github.barteksc:android-pdf-viewer:master-SNAPSHOT'
```

Или конкретный коммит:
```gradle
implementation 'com.github.barteksc:android-pdf-viewer:COMMIT_SHA'
```

### Вариант 3: Проверить доступные версии

1. Откройте: https://jitpack.io/#barteksc/AndroidPdfViewer
2. Проверьте доступные версии/теги
3. Используйте последнюю доступную версию

## 📋 Проверка репозитория

Убедитесь, что JitPack добавлен в `settings.gradle`:

```gradle
dependencyResolutionManagement {
    repositoriesMode.set(RepositoriesMode.FAIL_ON_PROJECT_REPOS)
    repositories {
        google()
        mavenCentral()
        maven { url 'https://jitpack.io' }  // ✅ Должен быть
    }
}
```

## 🔧 Дополнительные шаги

1. **Очистите кэш Gradle:**
   ```bash
   gradlew clean
   ```

2. **Обновите зависимости:**
   ```bash
   gradlew --refresh-dependencies
   ```

3. **Пересоберите проект:**
   ```bash
   gradlew build
   ```

## ⚠️ Важно о поддержке 16 КБ

- Версия 2.8.2 может не поддерживать 16 КБ страницы напрямую
- AGP 8.13.0 должен автоматически выровнять библиотеки при сборке
- После сборки проверьте через APK Analyzer, что выравнивание 16384

## 📞 Альтернативы

Если библиотека barteksc не поддерживает 16 КБ, рассмотрите:
- Обновление до последней версии из GitHub
- Использование другой библиотеки для PDF
- Создание форка с поддержкой 16 КБ

